version: '3.3'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # map internal port 5432 to host port 6543
      - 6543:5432
      - 6544:5433
    restart: always
    volumes:
      - ./templatedb:/tmp/templatedb
